apiVersion: v1
kind: Service
metadata:
  name: tienda-web
  labels:
    app: tienda-web
    modulo: "5"
spec:
  type: NodePort
  selector:
    app: tienda-web
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30081
