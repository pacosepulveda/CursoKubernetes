apiVersion: v1
kind: Service
metadata:
  name: catalog-web-nodeport
  namespace: svc-troubleshooting
  labels:
    app: catalog-web
    exercise: troubleshooting
spec:
  type: NodePort
  selector:
    app: catalog-web
    component: frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30081
      protocol: TCP
