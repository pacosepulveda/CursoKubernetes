apiVersion: v1
kind: Service
metadata:
  name: my-nginx-loadbalancer
  namespace: my-nginx
spec:
  type: LoadBalancer
  selector:
    app: my-nginx
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
