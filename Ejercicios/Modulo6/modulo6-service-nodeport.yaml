apiVersion: v1
kind: Service
metadata:
  name: my-nginx-nodeport
  namespace: my-nginx
spec:
  type: NodePort
  selector:
    app: my-nginx
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
      nodePort: 30080
